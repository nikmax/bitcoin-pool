services:
  btc_pool:
    image: python:3.11-slim
    container_name: btc-pool
    restart: unless-stopped
    volumes:
      - ./:/btc
    ports:
      - "8080:8080"
    command:
      - /btc/start.sh
